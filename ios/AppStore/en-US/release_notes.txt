V Bot build 59: W3 Local VM premium — viewer reliability, input, and screenshot polish.

• Local VM create/start and viewer connect stay reliable across reconnects
• Screenshot fallback when the live stream stalls
• Viewer ticket refresh and rotation without forcing a full reload
• Touch and Trackpad input modes switch without reloading the viewer
• Save to Photos is add-only (no broad library read access)
• Localized connection retry banner when the viewer drops
• Return-to-chat composer restore regression check from builds 56–58
